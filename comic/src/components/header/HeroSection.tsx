import React from 'react';
import styles from '../../styles/Home.module.css';

export const HeroSection = () => {
	return (
		<section className={styles.heroSection}>
			<h1 className={styles.heroHeadline}>Comic Closet</h1>
		</section>
	);
};
